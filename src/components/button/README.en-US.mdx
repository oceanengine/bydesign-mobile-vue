# Button

### Install

:::card

```javascript
import { Button } from '@byted/by-design-m';

Vue.use(Button);
```

:::

## Usage

:::card

```topic
Type
```

```html
<byted-button type="default">Default</byted-button>
<byted-button type="primary">Primary</byted-button>
<byted-button type="info">Info</byted-button>
<byted-button type="danger">Danger</byted-button>
<byted-button type="warning">Warning</byted-button>
```

:::

:::card

```topic
Plain
```

```html
<byted-button plain text="Plain" type="primary" />
<byted-button plain text="Plain" type="success" />
<byted-button plain text="Plain" type="warning" />
<byted-button plain text="Plain" type="danger" />
```

:::

:::card

```topic
Fade
```

```html
<byted-button fade text="Fade" type="primary" />
<byted-button fade text="Fade" type="success" />
<byted-button fade text="Fade" type="warning" />
<byted-button fade text="Fade" type="danger" />
```

:::

:::card

```topic
Hairline
```

```html
<byted-button fade text="fade" type="primary" />
<byted-button fade text="success" type="success" />
<byted-button fade text="warning" type="warning" />
<byted-button fade text="danger" type="danger" />
```

:::

:::card

```topic
Shape
```

```html
<byted-button square type="primary">Square</byted-button>
<byted-button round type="danger">Round</byted-button>
```

:::

:::card

```topic
Disabled
```

```html
<byted-button disabled type="primary">Diabled</byted-button>
<byted-button disabled type="danger">Diabled</byted-button>
```

:::

:::card

```topic
Size
```

```html
<byted-button type="primary" size="large">Large</byted-button>
<byted-button type="primary" size="normal">Normal</byted-button>
<byted-button type="primary" size="small">Small</byted-button>
<byted-button type="primary" size="mini">Mini</byted-button>
<byted-button type="primary" size="tiny">Tiny</byted-button>
```

:::

:::card

```topic
Text
```

```html
<byted-button text="large" size="large" type="text" />
<byted-button text="normal" type="text" />
<byted-button text="small" size="small" type="text" />
<byted-button text="mini" size="mini" type="text" />
<byted-button text="tiny" size="tiny" type="text" />
<byted-button text="large" size="large" type="text" disabled />
```

:::

## API

### Props

| Attribute | Description                                              | Type      | Default   |
| --------- | -------------------------------------------------------- | --------- | --------- |
| type      | Can be set to `primary` `info` `warning` `danger` `text` | `string`  | `default` |
| size      | Can be set to `large` `small` `mini`                     | `string`  | `normal`  |
| text      | Text                                                     | `string`  | -         |
| icon      | Left Icon                                                | `string`  | -         |
| plain     | Whether to be plain button                               | `boolean` | `false`   |
| round     | Whether to be round button                               | `boolean` | `false`   |
| square    | Whether to be square button                              | `boolean` | `false`   |
| disabled  | Whether to disable button                                | `boolean` | `false`   |
| loading   | Whether to loading button                                | `boolean` | `false`   |

### Events

| Event      | Description                                             | Arguments         |
| ---------- | ------------------------------------------------------- | ----------------- |
| click      | Triggered when click button and not disabled or loading | event: Event      |
| touchstart | Triggered when touch start                              | event: TouchEvent |
