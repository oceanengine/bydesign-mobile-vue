# Header

### Install

:::card

```javascript
import { Header } from '@byted/by-design-m';

Vue.use(Header);
```

:::

## Usage

:::card

```topic
Basic Usage
```

```html
<byted-header title="Title" @click-left="onClickLeft" />
```

```javascript
export default {
  methods: {
    onClickLeft() {
      Toast('Back');
    },
  },
};
```

:::

:::card

```topic
Fixed header
```

```html
<byted-header title="Title" fixed @click-left="back" />
```

:::

:::card

```topic
No button
```

```html
<byted-header title="ByDesign" :left-arrow="false" />
```

:::

:::card

```topic
Custom button
```

```html
<byted-header title="ByDesign">
  <byted-button slot="left" text="cancle" type="text" />
  <byted-button slot="right" text="confirm" type="text" />
</byted-header>
```

:::

:::card

```topic
Style
```

```html
<byted-header title="Title" type="primary" />
```

:::

:::card

```topic
Advanced Usage
```

```html
<byted-header title="Title">
  <byted-icon name="check-one" slot="left" />
  <byted-icon name="radio-uncheck" slot="right" />
</byted-header>
<byted-header title="ByDesign" type="primary">
  <byted-header-tabs v-model="type">
    <byted-header-tab title="字节跳动" :name="0" />
    <byted-header-tab name="1"> 巨量引擎 </byted-header-tab>
  </byted-header-tabs>
</byted-header>
```

:::

## API

### Props

| Attribute  | Description                                      | Type      | Default   |
| ---------- | ------------------------------------------------ | --------- | --------- |
| type       | Can be set to `primary`                          | `string`  | `default` |
| title      | Title                                            | `string`  | `''`      |
| left-arrow | Whether to show left arrow                       | `boolean` | `false`   |
| border     | when `type` === `default` Whether to show border | `boolean` | `true`    |
| fixed      | Whether to fixed top                             | `boolean` | `false`   |

### Slots

| Name  | Description               |
| ----- | ------------------------- |
| title | Custom title              |
| left  | Custom left side content  |
| right | Custom right side content |

### Events

| Event       | Description                       | Arguments |
| ----------- | --------------------------------- | --------- |
| click-left  | Triggered when click left button  | -         |
| click-right | Triggered when click right button | -         |
